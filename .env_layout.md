# --- Configurazione Generale ---
PROJECT_NAME="RAG Ollama FastAPI Test"
ENVIRONMENT="development"
DEBUG=True

# --- Configurazione Ollama (Aggiornato per Docker Compose) ---
# Usa il nome del servizio "ollama" invece di localhost
OLLAMA_BASE_URL="http://ollama:11434"
OLLAMA_MODEL_NAME="llama3"

# --- Configurazione Database Vettoriale Qdrant  ---
# Usa il nome del servizio "qdrant" invece di localhost
QDRANT_HOST="qdrant"
QDRANT_PORT="6333"
QDRANT_COLLECTION_NAME="rag_ollama_collection"

# --- Configurazione Embedding e Chunking ---
EMBEDDING_MODEL_NAME="nomic-embed-text"
CHUNK_SIZE=1000
CHUNK_OVERLAP=200